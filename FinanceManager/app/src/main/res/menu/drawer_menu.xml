<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:showIn="navigation_view">

    <item android:id="@+id/action_search"
        android:icon="@drawable/ic_search"
        android:title="Search"
        app:showAsAction="ifRoom|collapseActionView"
        app:actionViewClass="androidx.appcompat.widget.SearchView"/>

<!-- Menüpunkte in der Navigation, diese werden im nav_header.xml gebunden -->
    <group
        android:checkableBehavior="single"
        android:id="@+id/groupItem">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/ic_home"
            android:title="Startseite" />
        <item
            android:id="@+id/nav_chart"
            android:icon="@drawable/ic_chart"
            android:title="Statistik" />
        <item
            android:id="@+id/nav_search"
            android:icon="@drawable/ic_search"
            android:title="Erweiterte Suche" />
        <item
            android:id="@+id/nav_impressum"
            android:icon="@drawable/black_info"
            android:title="Impressum" />
    </group>

</menu>